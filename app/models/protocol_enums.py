from enum import IntEnum

class E_PrimaryKeypadFunctionMode(IntEnum):
    Disarm = 1
    ArmInAwayMode = 2
    ArmInStayMode = 3
    Cancel = 4
    AutoArm = 5
    # Aggiungi altri valori se necessario
